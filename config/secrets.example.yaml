# Configuration & Secrets Example Template
# Copy or merge this structure into config/secrets.yaml

zerodha:
  api_key: "YOUR_ZERODHA_API_KEY"
  api_secret: "YOUR_ZERODHA_API_SECRET"
  access_token: "" # Generated automatically upon login via the dashboard

gemini:
  api_key: "YOUR_GEMINI_API_KEY" # Optional: provides AI narrative commentary. If omitted, uses deterministic rule-based engine.
